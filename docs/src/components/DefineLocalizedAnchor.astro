---
import { transform } from "esbuild";

import LocalizedAnchorRaw from "./LocalizedAnchor.ts?raw";

export async function generateCode() {
  const { code } = await transform(LocalizedAnchorRaw, {
    loader: "ts",
    minify: true,
    target: "esnext",
  });
  return code;
}

const code = await generateCode();
---

<script is:inline set:html={code} />
