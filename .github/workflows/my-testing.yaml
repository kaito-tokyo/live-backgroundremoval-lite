name: "Tests"

on:
  push:
    branches:
      - "main"
  pull_request:
    branches:
      - "main"

concurrency:
  group: "${{ github.workflow }}-${{ github.ref }}"
  cancel-in-progress: true

defaults:
  run:
    shell: "bash"

permissions:
  contents: "read"
  packages: "write"

jobs:
  RunTests:
    name: "Run Tests on ${{ matrix.os-name }}"
    strategy:
      matrix:
        include:
          - os: macos-15
            os-name: macOS
            preset: macos-testing-ci
          - os: windows-2022
            os-name: Windows
            preset: windows-testing-ci-x64
          - os: ubuntu-24.04
            os-name: Ubuntu
            preset: ubuntu-testing-ci-x86_64
      fail-fast: false

    runs-on: ${{ matrix.os }}
    timeout-minutes: 30

    env:
      VCPKG_BINARY_SOURCES: "clear;nuget,https://nuget.pkg.github.com/kaito-tokyo/index.json,readwrite"
      VCPKG_FEATURE_FLAGS: "credentialprovider"

    steps:
      - name: "Checkout"
        uses: "actions/checkout@v5"

      - name: "Setup .NET"
        uses: "actions/setup-dotnet@v4"
        with:
          dotnet-version: '8.0.x'

      - name: "Setup NuGet Credentials"
        shell: "pwsh"
        run: |
          dotnet nuget add source "https://nuget.pkg.github.com/kaito-tokyo/index.json" --name "GitHub" --username "kaito-tokyo" --password "${{ secrets.GITHUB_TOKEN }}" --store-password-in-clear-text

      - name: "Install dependencies on macOS"
        if: runner.os == 'macOS'
        run: |
          brew install mono

      - name: "Install dependencies on Ubuntu"
        if: runner.os == 'Linux'
        run: |
          echo "man-db man-db/auto-update boolean false" | sudo debconf-set-selections
          sudo apt-get remove --purge -y man-db
          sudo add-apt-repository -y ppa:obsproject/obs-studio
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libgles2-mesa-dev \
            obs-studio \
            qt6-base-dev \
            libqt6svg6-dev \
            qt6-base-private-dev \
            cmake \
            git \
            jq \
            ninja-build \
            pkg-config \
            mono-runtime

      - name: "Configure"
        run: "cmake --preset ${{ matrix.preset }}"

      - name: "Build"
        run: "cmake --build --preset ${{ matrix.preset }}"
        
      - name: "Run tests"
        run: "ctest --preset ${{ matrix.preset }} --verbose"
